# ===========================================
# 数据库配置
# ===========================================
# SQLite 数据库路径 (相对于项目根目录)
DATABASE_URL="file:./dev.db"

# ===========================================
# 服务器配置
# ===========================================
# 服务器端口
PORT=4000

# 运行环境 (development | production | test)
NODE_ENV=production

# ===========================================
# 图片和资源配置
# ===========================================
# 图片服务的基础URL，用于拼接完整的图片链接
# 开发环境: http://localhost:4000
# Docker部署: http://localhost:8080/public (或实际的域名和端口)
IMAGE_BASE_URL="http://localhost:4000"

# ===========================================
# 日志配置
# ===========================================
# 日志级别 (DEBUG | INFO | WARNING | ERROR)
# DEBUG: 显示所有日志信息，适用于开发环境
# INFO: 显示一般信息和错误，适用于生产环境
# WARNING: 仅显示警告和错误
# ERROR: 仅显示错误信息
LOG_LEVEL=DEBUG

# 控制台输出开关 (true | false)
# 控制是否将日志输出到控制台
# 开发环境默认开启，生产环境默认关闭
# 在生产环境中可设置为 true 来启用控制台输出用于调试
CONSOLE_OUTPUT=false

# ===========================================
# API 配置
# ===========================================
# API 请求超时时间 (毫秒)
API_TIMEOUT=10000

# ===========================================
# 生产环境示例配置
# ===========================================
# NODE_ENV=production
# LOG_LEVEL=INFO
# CONSOLE_OUTPUT=false
# IMAGE_BASE_URL="https://your-domain.com"
# PORT=4000